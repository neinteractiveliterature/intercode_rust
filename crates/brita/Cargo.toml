[package]
edition = "2021"
name = "brita"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
liquid = {workspace = true}
liquid-core = {workspace = true}
liquid-lib = {workspace = true}
once_cell = {workspace = true}
